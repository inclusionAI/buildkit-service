/source.zip
/cmd/buildctl-daemon/test/source.zip
/.buildctl-batch.state
/scripts/.buildctl-batch.state
/result.jsonl
/bin/buildctl-batch
/source
/bin/buildctl-daemon
/buildctl-daemon
/infra/

# Production-specific Helm override values, keep local only.
/chart/values-prod.yaml

# Local infrastructure state and credentials must never be committed.
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
kubeconfig
kubeconfig.*
__pycache__/
*.pyc

# Local end-to-end runs: logs, isolated kubeconfigs and Docker state.
/artifacts/
/.venv/
